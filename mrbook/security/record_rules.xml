<odoo>
    <data>

        <record id="rule_mrbook_booking_user" model="ir.rule">
            <field name="name">Booking: Users see own bookings</field>
            <field name="model_id" ref="model_mrbook_booking"/>
            <field name="groups" eval="[(4, ref('mrbook.group_mrbook_user'))]"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <record id="rule_mrbook_booking_manager" model="ir.rule">
            <field name="name">Booking: Managers see all bookings</field>
            <field name="model_id" ref="model_mrbook_booking"/>
            <field name="groups" eval="[(4, ref('mrbook.group_mrbook_manager'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

    </data>
</odoo>